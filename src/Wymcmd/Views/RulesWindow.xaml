<Window x:Class="Wymcmd.Views.RulesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:Wymcmd.Core.Localization"
        Title="Why My CMD Opened"
        Width="760" Height="620"
        Background="{StaticResource Background}"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="{loc:T gui.rules_title}" FontSize="17" FontWeight="SemiBold" />
            <TextBlock Text="{loc:T gui.rules_hint}" Style="{StaticResource Label}" TextWrapping="Wrap" Margin="0,4,0,0" />
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding Rules}" SelectedItem="{Binding Selected}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <CheckBox Grid.Column="0" IsChecked="{Binding Enabled, Mode=TwoWay}"
                                  VerticalAlignment="Center" Margin="0,0,10,0" />

                        <StackPanel Grid.Column="1">
                            <TextBlock>
                                <Run Text="{Binding Title, Mode=OneWay}" FontWeight="SemiBold" />
                                <Run Text="{Binding Action, Mode=OneWay}" Foreground="{StaticResource Cyan}" />
                            </TextBlock>
                            <TextBlock Text="{Binding Conditions}" Style="{StaticResource Label}" TextWrapping="Wrap" />
                            <TextBlock Text="{Binding DryRun}" Style="{StaticResource Label}"
                                       Foreground="{StaticResource Warn}" />
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock Grid.Row="2" Text="{Binding Message}" Foreground="{StaticResource Mint}"
                   TextWrapping="Wrap" Margin="0,12,0,0" />

        <WrapPanel Grid.Row="3" Margin="0,14,0,0">
            <Button Content="{loc:T gui.rule_from_selection}" Command="{Binding AddFromSelectionCommand}"
                    Style="{StaticResource AccentButton}" IsEnabled="{Binding CanSeed}" Margin="0,0,8,8"
                    ToolTip="{Binding SeedDescription}" />
            <Button Content="{loc:T gui.rule_save}" Command="{Binding SaveCommand}" Margin="0,0,8,8" />
            <Button Content="{loc:T gui.rule_reload}" Command="{Binding ReloadCommand}" Margin="0,0,8,8" />
            <Button Content="{loc:T gui.rule_remove}" Command="{Binding RemoveCommand}"
                    Foreground="{StaticResource Danger}" Margin="0,0,0,8" />
        </WrapPanel>
    </Grid>
</Window>
